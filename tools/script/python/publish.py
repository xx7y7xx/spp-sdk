# coding=utf-8import os,sysimport shutilimport configimport common# # ########################### ######检索模板转角控制权# ##############def ifTemplate (e):	if e :		tmpDir  = "%s\\template\\%s\\"%(config.SPP_HOME, e)				if os.path.isdir(tmpDir):		# 判断 target 目录是否存在						sys.path.append(tmpDir)			import build			# 导入数据构建模块				import xml2json		#导入数据转换模块						# 执行数据构建操作			build.build(tmpDir)				else:			print common.encodeChinese('..........请选择模板（例如已有的模板：game、school等.......）')		